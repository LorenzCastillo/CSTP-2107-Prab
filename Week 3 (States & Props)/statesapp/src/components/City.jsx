/* eslint-disable react/prop-types */
const City = (props) => {
    return (
        <>
            <div>
                {props.cityName}
            </div>
        </>
    );
}

export default City;